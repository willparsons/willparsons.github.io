/** @type {import('prettier').Config} */
const config = {
    printWidth: 120,
    useTabs: false,
    tabWidth: 4,
    semi: true,
    singleQuote: false,
    trailingComma: "all",
};

export default config;
